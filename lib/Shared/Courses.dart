import '../Models/Course.dart';

class SharedCourses {
/*  static List<Course> sharedCourses = [];
  static bool containsCourse(Course c){
    return sharedCourses.indexWhere((element) => element.id == c.id) == -1;
  }
  static void addCourse(Course c) {
    if (!containsCourse(c)) {
      sharedCourses.add(c);
    }
  }*/
  /*static void updateCourse(Course c) {
    sharedCourses.firstWhere((element) => element.id==c.id).fromJson(c.toJson());
  }
  static void deleteCourse(Course c) {
    if (!containsCourse(c)) {
      sharedCourses.removeWhere((e)=>e.id == c.id);
    }
  }*/
}
